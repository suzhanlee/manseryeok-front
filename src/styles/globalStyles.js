import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#faf5ff',
  },
  button: {
    padding: 15,
    borderRadius: 15,
    backgroundColor: '#fff',
    alignItems: 'center',
  }
});